name = "analyzer"
description = "Use when the parity-test-f pipeline needs to read a pull request diff file and identify issues (null checks, error handling, naming) for the reviewer step."
model = "gpt-5.4-mini"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
version = "1.0.0"
plugin_version = "2.0.0"
skills = ["analyzer-protocol"]
